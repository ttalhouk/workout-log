<div class="col-md-12">
  <% flash.each do |key, msg|%>
    <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %> text-center">
      <strong><%= msg %></strong>
    </div>
  <% end %>

</div>
